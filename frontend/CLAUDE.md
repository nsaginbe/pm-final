# Техническое задание для Flutter-разработчика: Мультиагентная торговая система

## ⚠️ ВАЖНО: Официальные требования к проекту

Это учебный проект с официальными критериями оценки. Backend разработан в соответствии со всеми требованиями ниже. **Твоя задача как Flutter-разработчика — создать UI, который наглядно демонстрирует выполнение всех этих критериев.**

### Обязательные критерии проекта (для сдачи)

**1. Три агента (ОБЯЗАТЕЛЬНО)**
- ✅ **Market Monitor** – наблюдает за ценами
- ✅ **Decision Maker** – принимает решения BUY, SELL или HOLD
- ✅ **Execution Agent** – выполняет сделки

**2. Агенты должны взаимодействовать друг с другом**
- ✅ Market Monitor отправляет данные о ценах
- ✅ Decision Maker отправляет инструкции
- ✅ Execution Agent подтверждает действие

**3. Система должна показывать чёткий процесс**
- ✅ Мониторинг рынка
- ✅ Принятие решения
- ✅ Исполнение сделки

**4. Система должна работать**
- ✅ Использует реальные или симулированные рыночные данные
- ✅ Принимает логические решения
- ✅ Сохраняет или записывает сделки
- ✅ Обрабатывает ошибки и отсутствующие данные

**5. Требования к коду**
- ✅ Рабочая программа или демо
- ✅ Каждый агент написан отдельно
- ✅ Организованный и понятный код

**6. Требования к презентации**
- ✅ Показать взаимодействие агентов
- ✅ Показать поток данных и решения
- ✅ Показать финальное исполнение сделки
- ✅ Отобразить логи или простой UI

**7. Документация должна включать**
- ✅ О чём проект
- ✅ Что делает каждый агент
- ✅ Как они взаимодействуют
- ✅ Как работает система
- ✅ Инструкции по запуску

### 🎯 КРИТИЧНО ДЛЯ FLUTTER UI

Твоё приложение должно **визуально демонстрировать**:

1. **Три отдельных агента** - создай отдельные визуальные секции/карточки для каждого агента
2. **Коммуникацию между агентами** - покажи стрелки, индикаторы или анимации передачи данных
3. **Поток данных** - Market Monitor → Decision Maker → Execution Agent
4. **Логи каждого агента** - что он получил, что сделал, что отправил дальше
5. **Результаты** - финальная сделка с деталями

Пример визуализации на Dashboard:
```
┌─────────────────────────────────┐
│   MARKET MONITORING AGENT       │
│   Status: ✓ Active              │
│   Current Price: $43,210        │
│   Indicators: SMA_10, SMA_50... │
│   ↓ Sending to Decision Maker   │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   DECISION MAKING AGENT         │
│   Status: ✓ Processing          │
│   Decision: BUY (84% confidence)│
│   Reason: Price above SMA_50    │
│   ↓ Sending to Execution Agent  │
└─────────────────────────────────┘
           ↓
┌─────────────────────────────────┐
│   EXECUTION AGENT               │
│   Status: ✓ Executed            │
│   Action: BUY at $43,212        │
│   Order ID: ORD-987654          │
│   Status: FILLED                │
└─────────────────────────────────┘
```

---

## Обзор проекта

Ты выступаешь как senior Flutter-разработчик. Твоя задача — создать мобильное приложение для учебного проекта мультиагентной системы автоматической торговли с использованием искусственного интеллекта. Backend уже разработан на Python (FastAPI) и реализует сложную мультиагентную архитектуру для симуляции автоматической торговли в полном соответствии с официальными требованиями проекта.

### Что делает система

Система симулирует автоматическую торговлю криптовалютой с использованием трёх AI-агентов, которые работают вместе:
1. **Market Monitoring Agent** (Агент мониторинга рынка) — собирает данные с Binance в реальном времени
2. **Decision-Making Agent** (Агент принятия решений) — использует ML-модели для принятия решений BUY/SELL/HOLD
3. **Execution Agent** (Агент исполнения) — симулирует исполнение сделок и записывает результаты

Backend оркестрирует этих агентов последовательно и предоставляет данные в реальном времени через REST API.

---

## Требования к технологическому стеку

### Основные технологии
- **Фреймворк**: Flutter (последняя стабильная версия)
- **Язык**: Dart
- **State Management**: Provider или Riverpod (выбери наиболее подходящий)
- **HTTP клиент**: пакет `dio` для API-запросов
- **JSON сериализация**: `json_serializable` + `freezed` для типобезопасных моделей
- **Графики**: `fl_chart` для визуализации цен и индикаторов
- **Локальное хранилище**: `shared_preferences` для настроек

### Дополнительные пакеты (при необходимости)
- `intl` — для форматирования даты/времени
- `flutter_dotenv` — для конфигурации окружения
- `logger` — для отладки
- `equatable` — для сравнения значений

---

## Спецификация Backend API

Backend предоставляет следующие эндпоинты:

### 1. `POST /trading/run-cycle`

**Назначение**: Запускает один полный торговый цикл (мониторинг → решение → исполнение)

**Параметры запроса**:
- `symbol` (опционально): Торговая пара, по умолчанию: "BTCUSDT"

**Формат ответа**:
```json
{
  "cycle_id": 17,
  "timestamp": "2025-11-29T14:32:10Z",
  "market_data": {
    "symbol": "BTCUSDT",
    "price": 43210.55,
    "indicators": {
      "sma_10": 43100.12,
      "sma_50": 42050.44,
      "rsi_14": 62.4
    },
    "source": "binance"
  },
  "decision": {
    "action": "BUY",
    "confidence": 0.84,
    "model_version": "v1.0.3",
    "reason": "Price above SMA_50, positive trend detected"
  },
  "execution": {
    "executed": true,
    "execution_price": 43212.00,
    "order_id": "ORD-20251129-987654",
    "status": "FILLED",
    "time": "2025-11-29T14:32:11Z"
  },
  "logs": {
    "market_agent": "Received live price and calculated indicators",
    "decision_agent": "Model predicted BUY with 0.84 confidence",
    "execution_agent": "Trade executed successfully"
  }
}
```

### 2. `GET /trades` (Опционально)

**Назначение**: Получить историю симулированных сделок

**Формат ответа**:
```json
[
  {
    "id": 1,
    "symbol": "BTCUSDT",
    "action": "BUY",
    "price": 43210.55,
    "execution_price": 43212.00,
    "timestamp": "2025-11-29T14:32:10Z",
    "status": "FILLED",
    "order_id": "ORD-20251129-987654"
  },
  ...
]
```

### 3. `GET /market/latest` (Опционально)

**Назначение**: Получить текущие рыночные данные без запуска торгового цикла

**Query параметры**:
- `symbol` (опционально): Торговая пара, по умолчанию: "BTCUSDT"

**Формат ответа**:
```json
{
  "symbol": "BTCUSDT",
  "price": 43210.55,
  "indicators": {
    "sma_10": 43100.12,
    "sma_50": 42050.44,
    "rsi_14": 62.4
  },
  "timestamp": "2025-11-29T14:32:10Z"
}
```

---

## Требуемый функционал приложения

### Основной функционал

#### 1. Экран Dashboard (Главный экран)
**Назначение**: Отображение статуса торговой системы в реальном времени и результатов последнего цикла

**⚠️ КРИТИЧНЫЕ ТРЕБОВАНИЯ для соответствия критериям проекта:**

**Визуализация трёх агентов:**
- Создай **три отдельные карточки/секции** для каждого агента:
  1. Market Monitoring Agent Card
  2. Decision Making Agent Card  
  3. Execution Agent Card
- Каждая карточка должна показывать:
  - Название агента
  - Статус (Active/Processing/Completed)
  - Входные данные (что агент получил)
  - Выходные данные (что агент отправил)
  - Текущее действие агента

**Визуализация коммуникации:**
- Анимированные стрелки или индикаторы показывающие поток данных между агентами
- Или пронумерованные шаги (Step 1, Step 2, Step 3) с подсветкой активного
- Или timeline визуализация процесса

**Визуализация процесса (чёткий flow):**
```
[Monitor] → [Decide] → [Execute]
```
Пользователь должен видеть:
- ШАГ 1: Market Monitor собирает данные
- ШАГ 2: Decision Maker анализирует и решает
- ШАГ 3: Execution Agent исполняет

**Обязательные элементы**:
- Текущая цена BTC/USDT (крупное, заметное отображение)
- Последнее торговое решение (BUY/SELL/HOLD) с индикатором уверенности
- Визуальный индикатор уверенности (progress bar или gauge, показывающий 0-100%)
- Отображение причины действия (текстовое объяснение от ML-модели)
- Детали последнего исполнения (цена, статус, ID ордера)
- Панель технических индикаторов:
  - Значение SMA 10
  - Значение SMA 50
  - Значение RSI 14
  - Визуальное представление тренда
- Кнопка "Запустить торговый цикл" (основное действие)
- ID цикла и временная метка последнего обновления
- Переключатель автообновления (опционально)

**Требования к UI/UX**:
- Цветовая кодировка для действий:
  - BUY = Зелёный
  - SELL = Красный
  - HOLD = Серый/Жёлтый
- Состояние загрузки во время API-вызова
- Обработка ошибок с понятными сообщениями
- Поддержка жеста pull-to-refresh

#### 2. Экран истории сделок
**Назначение**: Отображение списка всех симулированных сделок

**Обязательные элементы**:
- Прокручиваемый список прошлых сделок
- Для каждой сделки отображать:
  - Торговое действие (BUY/SELL/HOLD) с иконкой
  - Символ (например, BTCUSDT)
  - Цена исполнения
  - Временная метка (красиво отформатированная)
  - Бейдж статуса (FILLED/SKIPPED/REJECTED)
  - ID ордера
- Фильтр по типу действия (BUY/SELL/HOLD)
- Сортировка по дате (новые/старые)
- Модальное окно с деталями сделки при нажатии

**Требования к UI/UX**:
- Использовать ListView.builder для производительности
- Pull-to-refresh для перезагрузки из API
- Пустое состояние когда нет сделок
- Плавные анимации для элементов списка

#### 3. Экран визуализации рыночных данных
**Назначение**: Отображение трендов рынка и технических индикаторов

**Обязательные элементы**:
- График цен (линейный график с историей цен)
- Наложение индикаторов SMA на график цен
- График RSI (отдельный график под графиком цен)
- Карточка текущей рыночной статистики
- Селектор символа (BTCUSDT, ETHUSDT и т.д.)
- Селектор таймфрейма (интервалы 1m, 5m, 15m)

**Требования к UI/UX**:
- Использовать пакет `fl_chart` для графиков
- Интерактивные графики с сенсорными жестами
- Легенда для различных индикаторов
- Скелетон загрузки при получении данных

#### 4. Экран логов агентов
**Назначение**: Отображение детальных логов от каждого агента в последнем цикле

**⚠️ КРИТИЧНО: Этот экран демонстрирует взаимодействие агентов для оценки проекта**

**Обязательные элементы**:
- **Три отдельные секции** (сворачиваемые карточки):
  1. **Market Monitoring Agent Logs**
     - Показать: "Received request to monitor BTCUSDT"
     - Показать: "Fetched current price: $43,210.55"
     - Показать: "Calculated indicators: SMA_10, SMA_50, RSI_14"
     - Показать: "→ Sending data to Decision Maker"
  
  2. **Decision-Making Agent Logs**
     - Показать: "← Received market data from Monitor"
     - Показать: "Processing features: [price, sma_10, sma_50, rsi_14]"
     - Показать: "ML Model prediction: BUY (confidence: 0.84)"
     - Показать: "→ Sending decision to Execution Agent"
  
  3. **Execution Agent Logs**
     - Показать: "← Received decision: BUY from Decision Maker"
     - Показать: "Simulating trade execution..."
     - Показать: "Trade executed: BUY at $43,212.00"
     - Показать: "Order ID: ORD-20251129-987654, Status: FILLED"
     - Показать: "✓ Trade recorded to database"

**Визуализация коммуникации в логах:**
- Используй стрелки: `→` для "отправил данные", `←` для "получил данные"
- Цветовая кодировка:
  - Получение данных: синий цвет
  - Обработка: жёлтый цвет
  - Отправка данных: зелёный цвет
  - Ошибки: красный цвет

**Дополнительные элементы**:
- Временная метка для каждой записи лога
- Индикаторы уровня лога (если доступны)
- Функция копирования в буфер обмена

**Требования к UI/UX**:
- Моноширинный шрифт для текста логов
- Раскрывающиеся/сворачивающиеся секции
- Автопрокрутка к последним логам
- Функционал поиска/фильтрации

#### 5. Экран настроек
**Назначение**: Конфигурация поведения приложения

**Обязательные элементы**:
- Конфигурация базового URL API
- Выбор торгового символа по умолчанию
- Настройка интервала автообновления
- Переключатель темы (светлая/тёмная)
- Настройки уведомлений (будущее улучшение)
- Секция "О приложении" (версия, авторы)

---

## 📊 Режим презентации (для защиты проекта)

### ОБЯЗАТЕЛЬНО: Режим демонстрации

Так как проект будет презентоваться, создай **специальный режим презентации**:

**Кнопка "Presentation Mode" в настройках:**
- При активации этого режима:
  - Увеличить шрифты для лучшей видимости
  - Показывать анимацию потока данных между агентами
  - Автоматически раскрывать все логи
  - Подсвечивать активный шаг процесса

**Пошаговая демонстрация:**
Возможность запустить цикл в "медленном режиме" с паузами между шагами:
1. Показать Market Monitor в работе (3 сек)
2. Анимация передачи данных → (1 сек)
3. Показать Decision Maker в работе (3 сек)
4. Анимация передачи решения → (1 сек)
5. Показать Execution Agent в работе (3 сек)
6. Показать финальный результат

**Экран "System Flow" (дополнительно):**
Создай отдельный экран, показывающий архитектурную диаграмму:
```
┌──────────────┐
│   Binance    │
│  Market API  │
└──────┬───────┘
       │ Real-time data
       ↓
┌──────────────────────┐
│ Market Monitor Agent │
│  - Fetch prices      │
│  - Calculate SMA     │
│  - Calculate RSI     │
└──────┬───────────────┘
       │ Market data + indicators
       ↓
┌──────────────────────┐
│ Decision Maker Agent │
│  - Process features  │
│  - ML Model (sklearn)│
│  - Output: BUY/SELL  │
└──────┬───────────────┘
       │ Trading decision
       ↓
┌──────────────────────┐
│  Execution Agent     │
│  - Simulate trade    │
│  - Log to database   │
│  - Return result     │
└──────────────────────┘
```

---

## Модели данных (Dart классы)

Необходимо создать типобезопасные Dart-модели используя `freezed` и `json_serializable`. Вот требуемые модели:

### TradingCycleResponse
```dart
@freezed
class TradingCycleResponse with _$TradingCycleResponse {
  const factory TradingCycleResponse({
    required int cycleId,
    required DateTime timestamp,
    required MarketData marketData,
    required Decision decision,
    required Execution execution,
    required AgentLogs logs,
  }) = _TradingCycleResponse;

  factory TradingCycleResponse.fromJson(Map<String, dynamic> json) =>
      _$TradingCycleResponseFromJson(json);
}
```

### MarketData
```dart
@freezed
class MarketData with _$MarketData {
  const factory MarketData({
    required String symbol,
    required double price,
    required Indicators indicators,
    required String source,
  }) = _MarketData;

  factory MarketData.fromJson(Map<String, dynamic> json) =>
      _$MarketDataFromJson(json);
}
```

### Indicators
```dart
@freezed
class Indicators with _$Indicators {
  const factory Indicators({
    required double sma10,
    required double sma50,
    required double rsi14,
  }) = _Indicators;

  factory Indicators.fromJson(Map<String, dynamic> json) =>
      _$IndicatorsFromJson(json);
}
```

### Decision
```dart
@freezed
class Decision with _$Decision {
  const factory Decision({
    required String action, // "BUY" | "SELL" | "HOLD"
    required double confidence,
    required String modelVersion,
    required String reason,
  }) = _Decision;

  factory Decision.fromJson(Map<String, dynamic> json) =>
      _$DecisionFromJson(json);
}
```

### Execution
```dart
@freezed
class Execution with _$Execution {
  const factory Execution({
    required bool executed,
    required double executionPrice,
    required String orderId,
    required String status,
    required DateTime time,
  }) = _Execution;

  factory Execution.fromJson(Map<String, dynamic> json) =>
      _$ExecutionFromJson(json);
}
```

### AgentLogs
```dart
@freezed
class AgentLogs with _$AgentLogs {
  const factory AgentLogs({
    required String marketAgent,
    required String decisionAgent,
    required String executionAgent,
  }) = _AgentLogs;

  factory AgentLogs.fromJson(Map<String, dynamic> json) =>
      _$AgentLogsFromJson(json);
}
```

### Trade (для истории)
```dart
@freezed
class Trade with _$Trade {
  const factory Trade({
    required int id,
    required String symbol,
    required String action,
    required double price,
    required double executionPrice,
    required DateTime timestamp,
    required String status,
    required String orderId,
  }) = _Trade;

  factory Trade.fromJson(Map<String, dynamic> json) =>
      _$TradeFromJson(json);
}
```

---

## Слой API-сервиса

Создай выделенный API-сервис используя `dio`:

### Класс ApiService
```dart
class ApiService {
  final Dio _dio;
  final String baseUrl;

  ApiService({required this.baseUrl}) : _dio = Dio(BaseOptions(
    baseUrl: baseUrl,
    connectTimeout: Duration(seconds: 10),
    receiveTimeout: Duration(seconds: 10),
  ));

  // Требуемые методы:
  Future<TradingCycleResponse> runTradingCycle({String symbol = 'BTCUSDT'});
  Future<List<Trade>> getTradeHistory();
  Future<MarketData> getLatestMarketData({String symbol = 'BTCUSDT'});
}
```

**Требования**:
- Реализовать правильную обработку ошибок (try-catch)
- Обрабатывать сетевые ошибки корректно
- Парсить JSON-ответы используя фабрики моделей
- Логировать запросы/ответы для отладки
- Реализовать retry-логику для неудачных запросов (опционально)

---

## Архитектура управления состоянием

Используй **Provider** или **Riverpod** (на твой выбор) для управления состоянием приложения.

### Требуемые провайдеры/нотификаторы

1. **TradingCycleProvider**
   - Хранит данные текущего торгового цикла
   - Состояние загрузки
   - Состояние ошибки
   - Метод для запуска нового цикла

2. **TradeHistoryProvider**
   - Список исторических сделок
   - Состояние загрузки
   - Метод обновления

3. **MarketDataProvider**
   - Последние рыночные данные
   - Возможность автообновления
   - Состояние загрузки

4. **SettingsProvider**
   - Базовый URL API
   - Символ по умолчанию
   - Настройка темы
   - Интервал автообновления

---

## Руководство по дизайну UI/UX

### Принципы дизайна
- **Чистый и современный**: Использовать Material Design 3 (Material You)
- **Фокус на данных**: Приоритет читабельности цифр и графиков
- **Адаптивный**: Поддержка макетов для телефонов и планшетов
- **Доступный**: Правильные коэффициенты контрастности, поддержка масштабирования текста

### Цветовая схема
- **Основной цвет**: Синий/Бирюзовый (профессиональный, технологичный)
- **Цвета действий**:
  - BUY: Зелёный (#4CAF50)
  - SELL: Красный (#F44336)
  - HOLD: Янтарный (#FFC107)
- **Фон**: Светлый режим (белый/светло-серый) и поддержка тёмного режима

### Типографика
- **Заголовки**: Жирный шрифт, большие размеры
- **Основной текст**: Обычный вес, хорошая высота строки
- **Цифры**: Табличные цифры для выравнивания
- **Логи**: Моноширинный шрифт

### Отступы и макет
- Консистентные отступы (16px, 24px)
- Макет на основе карточек для группировки информации
- Правильное использование whitespace между секциями
- Адаптивная сетка для планшетов

---

## Требования к обработке ошибок

### Сетевые ошибки
- Отображать понятные сообщения об ошибках
- Показывать кнопку повтора
- Логировать технические ошибки в консоль
- Не допускать краша приложения

### Ошибки API
- Обрабатывать 4xx ошибки (bad request, not found)
- Обрабатывать 5xx ошибки (server errors)
- Показывать соответствующие сообщения пользователю
- Предоставлять fallback UI

### Граничные случаи
- Пустая история сделок
- Отсутствующие рыночные данные
- Медленные/timeout запросы
- Невалидные JSON-ответы

---

## Требования к тестированию

Реализуй базовые тесты:

1. **Widget-тесты**
   - Экран Dashboard рендерится корректно
   - Список сделок отображает элементы
   - Кнопки вызывают правильные действия

2. **Unit-тесты**
   - JSON-парсинг для всех моделей
   - Обработка ошибок в API-сервисе
   - Логика управления состоянием

3. **Integration-тесты** (опционально)
   - Полный флоу: запуск цикла → отображение результата
   - Навигация между экранами
   - Функционал pull-to-refresh

---

## Структура проекта

Организуй Flutter-проект следующим образом:

```
lib/
  main.dart
  
  models/
    trading_cycle.dart
    market_data.dart
    decision.dart
    execution.dart
    trade.dart
    agent_logs.dart
  
  services/
    api_service.dart
  
  providers/
    trading_cycle_provider.dart
    trade_history_provider.dart
    market_data_provider.dart
    settings_provider.dart
  
  screens/
    dashboard_screen.dart
    trade_history_screen.dart
    market_chart_screen.dart
    agent_logs_screen.dart
    system_flow_screen.dart      # NEW: Диаграмма архитектуры системы
    settings_screen.dart
  
  widgets/
    price_card.dart
    decision_card.dart
    indicator_card.dart
    trade_list_item.dart
    confidence_meter.dart
    action_badge.dart
    loading_indicator.dart
  
  utils/
    constants.dart
    formatters.dart
    theme.dart
  
  config/
    app_config.dart
```

---

## Приоритеты реализации

Создавай функционал в следующем порядке:

### Фаза 1 - MVP (Минимально жизнеспособный продукт)
1. Настроить структуру проекта
2. Создать модели данных с freezed
3. Реализовать API-сервис
4. Создать экран Dashboard с **визуализацией трёх агентов**
5. Реализовать кнопку "Запустить цикл" и отображение результатов
6. Создать экран логов агентов с **демонстрацией коммуникации**
7. Базовая обработка ошибок

### Фаза 2 - Основной функционал
1. Экран истории сделок
2. Pull-to-refresh на Dashboard
3. Экран настроек с конфигурацией API URL
4. **Режим презентации** с увеличенными шрифтами

### Фаза 3 - Расширенный функционал
1. Визуализация рыночных данных с графиками
2. Экран "System Flow" с архитектурной диаграммой
3. Анимации потока данных между агентами
4. Пошаговый режим демонстрации
5. Функционал автообновления
6. Поддержка тёмного режима

### Фаза 4 - Полировка
1. Плавные анимации и переходы
2. Комплексная обработка ошибок
3. Скелетоны загрузки
4. Unit и widget-тесты
5. Финальная подготовка к презентации

---

## Конфигурация и настройка

### Конфигурация окружения
Создай файл `.env` для конфигурации:
```
API_BASE_URL=http://localhost:8000
DEFAULT_SYMBOL=BTCUSDT
```

### Зависимости (pubspec.yaml)
```yaml
dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_riverpod: ^2.4.0  # или provider: ^6.1.0
  
  # Networking
  dio: ^5.4.0
  
  # JSON и сериализация
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  
  # Графики
  fl_chart: ^0.66.0
  
  # Утилиты
  intl: ^0.18.1
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.2
  logger: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Генерация кода
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  
  # Линтинг
  flutter_lints: ^3.0.0
```

---

## Best Practices и качество кода

### Стиль кода
- Следовать Dart style guide
- Использовать осмысленные имена переменных
- Писать комментарии для сложной логики
- Держать функции маленькими и сфокусированными
- Использовать const конструкторы где возможно

### Производительность
- Использовать `ListView.builder` для длинных списков
- Реализовать пагинацию для больших датасетов
- Кешировать API-ответы когда уместно
- Оптимизировать загрузку изображений
- Избегать ненужных перестроений

### Доступность
- Предоставлять семантические метки
- Поддерживать скринридеры
- Обеспечивать правильные коэффициенты контрастности
- Делать сенсорные цели минимум 48x48 dp

### Безопасность
- Не хардкодить API URL в коде
- Валидировать все пользовательские вводы
- Использовать HTTPS для production
- Правильно обрабатывать чувствительные данные

---

## Результаты работы

Необходимо предоставить:

1. **Полное Flutter-приложение** следующее структуре выше
2. **Рабочую интеграцию** с backend API
3. **Все 5 экранов** реализованы и функциональны
4. **Типобезопасные модели** используя freezed
5. **Слой API-сервиса** с обработкой ошибок
6. **Реализацию state management**
7. **Базовые тесты** (хотя бы widget-тесты для основных экранов)
8. **README.md** с:
   - Инструкциями по настройке
   - Как запустить приложение
   - Как настроить API URL
   - Скриншотами основных экранов
   - Известными проблемами/ограничениями

---

## Тестирование с Backend

### Локальная разработка
- Backend работает на `http://localhost:8000`
- Используй Android emulator или iOS simulator
- Для эмулятора используй `10.0.2.2:8000` вместо `localhost:8000`
- Для физического устройства используй локальный IP компьютера

### Чеклист тестирования
- [ ] Можно успешно запустить торговый цикл
- [ ] Все данные цикла отображаются корректно
- [ ] История сделок загружается и отображается
- [ ] Обработка ошибок работает (протестировать остановкой backend)
- [ ] Pull-to-refresh обновляет данные
- [ ] Настройки сохраняются между перезапусками приложения
- [ ] Графики рендерятся с реальными данными
- [ ] Приложение не падает на граничных случаях

---

## Дополнительные замечания

### Образовательный контекст
Это образовательный проект, демонстрирующий:
- Мультиагентные AI-системы
- Интеграцию данных рынка в реальном времени
- Принятие решений на основе ML
- Full-stack мобильную разработку

### Симуляция торговли
- БЕЗ реальных денег
- Все сделки симулированы
- Backend использует Binance public API только для данных
- Никакие реальные ордера не размещаются

### Будущие улучшения (Опционально)
- Обновления в реальном времени через WebSocket
- Множественные торговые пары
- Аналитика исторической производительности
- Конфигурация пользовательских стратегий
- Уведомления об исполнении сделок
- Экспорт истории сделок в CSV

---

## Критерии успеха

Твоя реализация будет считаться успешной если:

### Обязательные требования (для сдачи проекта):
✅ **Три агента визуально различимы** - каждый агент имеет свою карточку/секцию на UI
✅ **Показана коммуникация** - визуализирован поток данных: Monitor → Decision → Execution
✅ **Чёткий процесс** - пользователь видит три шага: мониторинг → решение → исполнение
✅ **Детальные логи** - логи каждого агента показывают что он получил и что отправил
✅ **Работает система** - можно запустить цикл и увидеть результат
✅ **Записываются сделки** - история сделок отображается и обновляется

### Технические требования:
✅ Все 5 основных экранов реализованы и функциональны
✅ Приложение успешно взаимодействует с backend API
✅ Модели данных типобезопасны с правильной JSON-сериализацией
✅ UI чистый, современный и user-friendly
✅ Обработка ошибок предотвращает крашы
✅ State management работает корректно
✅ Код хорошо организован и следует Flutter best practices
✅ Включены базовые тесты
✅ README-документация ясная и полная

### Презентационные требования:
✅ **Режим презентации** реализован (увеличенные шрифты, анимации)
✅ **Пошаговая демонстрация** работает корректно
✅ **Архитектурная диаграмма** показывает всю систему
✅ Приложение готово к демонстрации на защите проекта

---

## Получение помощи

Если возникнут проблемы:

1. **Проблемы с Backend API**: Проверь, что backend запущен на правильном порту
2. **CORS ошибки**: Backend должен иметь настроенный CORS для мобильных приложений
3. **Парсинг JSON**: Проверь, что поля моделей точно совпадают с ответом backend
4. **State Management**: Убедись, что провайдеры правильно обёрнуты в корне приложения
5. **Графики не рендерятся**: Проверь, что формат данных совпадает с требованиями `fl_chart`

---

## Финальное напоминание

Сфокусируйся на:
- **Функциональность прежде всего**, затем полировка
- **Чистая архитектура** и разделение ответственности
- **Пользовательский опыт** - приложение должно ощущаться профессиональным
- **Типобезопасность** - используй строгую типизацию Dart
- **Обработка ошибок** - изящные падения
- **Качество кода** - пиши поддерживаемый код

Удачи в создании отличного Flutter-фронтенда для этой мультиагентной торговой системы! 🚀

---

## Примеры экранов и компонентов

### Пример структуры Dashboard Screen

```dart
class DashboardScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final cycleState = ref.watch(tradingCycleProvider);
    
    return Scaffold(
      appBar: AppBar(
        title: Text('Trading Dashboard'),
        actions: [
          IconButton(
            icon: Icon(Icons.settings),
            onPressed: () => Navigator.push(...),
          ),
        ],
      ),
      body: RefreshIndicator(
        onRefresh: () => ref.read(tradingCycleProvider.notifier).refresh(),
        child: cycleState.when(
          loading: () => LoadingIndicator(),
          error: (error, stack) => ErrorWidget(error: error),
          data: (cycle) => SingleChildScrollView(
            child: Column(
              children: [
                PriceCard(marketData: cycle.marketData),
                DecisionCard(decision: cycle.decision),
                IndicatorCard(indicators: cycle.marketData.indicators),
                ExecutionCard(execution: cycle.execution),
                AgentLogsCard(logs: cycle.logs),
              ],
            ),
          ),
        ),
      ),
      floatingActionButton: FloatingActionButton.extended(
        onPressed: () => ref.read(tradingCycleProvider.notifier).runCycle(),
        label: Text('Запустить цикл'),
        icon: Icon(Icons.play_arrow),
      ),
    );
  }
}
```

### Пример PriceCard виджета

```dart
class PriceCard extends StatelessWidget {
  final MarketData marketData;
  
  const PriceCard({required this.marketData});
  
  @override
  Widget build(BuildContext context) {
    return Card(
      margin: EdgeInsets.all(16),
      child: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Text(
              marketData.symbol,
              style: Theme.of(context).textTheme.titleMedium,
            ),
            SizedBox(height: 8),
            Text(
              '\$${marketData.price.toStringAsFixed(2)}',
              style: Theme.of(context).textTheme.displayLarge?.copyWith(
                fontWeight: FontWeight.bold,
              ),
            ),
            SizedBox(height: 4),
            Text(
              'Источник: ${marketData.source}',
              style: Theme.of(context).textTheme.bodySmall,
            ),
          ],
        ),
      ),
    );
  }
}
```

### Пример DecisionCard виджета

```dart
class DecisionCard extends StatelessWidget {
  final Decision decision;
  
  const DecisionCard({required this.decision});
  
  Color _getActionColor() {
    switch (decision.action) {
      case 'BUY':
        return Colors.green;
      case 'SELL':
        return Colors.red;
      case 'HOLD':
      default:
        return Colors.amber;
    }
  }
  
  @override
  Widget build(BuildContext context) {
    return Card(
      margin: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
      child: Padding(
        padding: EdgeInsets.all(20),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                Text(
                  'Решение AI',
                  style: Theme.of(context).textTheme.titleMedium,
                ),
                Chip(
                  label: Text(
                    decision.action,
                    style: TextStyle(
                      color: Colors.white,
                      fontWeight: FontWeight.bold,
                    ),
                  ),
                  backgroundColor: _getActionColor(),
                ),
              ],
            ),
            SizedBox(height: 16),
            ConfidenceMeter(confidence: decision.confidence),
            SizedBox(height: 12),
            Text(
              'Причина:',
              style: Theme.of(context).textTheme.labelMedium,
            ),
            SizedBox(height: 4),
            Text(
              decision.reason,
              style: Theme.of(context).textTheme.bodyMedium,
            ),
            SizedBox(height: 8),
            Text(
              'Версия модели: ${decision.modelVersion}',
              style: Theme.of(context).textTheme.bodySmall,
            ),
          ],
        ),
      ),
    );
  }
}
```

### Пример ConfidenceMeter виджета

```dart
class ConfidenceMeter extends StatelessWidget {
  final double confidence;
  
  const ConfidenceMeter({required this.confidence});
  
  @override
  Widget build(BuildContext context) {
    final percentage = (confidence * 100).toInt();
    
    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Row(
          mainAxisAlignment: MainAxisAlignment.spaceBetween,
          children: [
            Text(
              'Уверенность',
              style: Theme.of(context).textTheme.labelMedium,
            ),
            Text(
              '$percentage%',
              style: Theme.of(context).textTheme.labelLarge?.copyWith(
                fontWeight: FontWeight.bold,
              ),
            ),
          ],
        ),
        SizedBox(height: 8),
        LinearProgressIndicator(
          value: confidence,
          backgroundColor: Colors.grey[300],
          color: confidence > 0.7 ? Colors.green : 
                 confidence > 0.4 ? Colors.orange : Colors.red,
          minHeight: 8,
          borderRadius: BorderRadius.circular(4),
        ),
      ],
    );
  }
}
```

### Пример API Service

```dart
class ApiService {
  final Dio _dio;
  final String baseUrl;
  final Logger _logger = Logger();

  ApiService({required this.baseUrl})
      : _dio = Dio(BaseOptions(
          baseUrl: baseUrl,
          connectTimeout: Duration(seconds: 10),
          receiveTimeout: Duration(seconds: 10),
          headers: {
            'Content-Type': 'application/json',
          },
        )) {
    // Добавляем логирование для отладки
    _dio.interceptors.add(LogInterceptor(
      requestBody: true,
      responseBody: true,
      logPrint: (obj) => _logger.d(obj),
    ));
  }

  Future<TradingCycleResponse> runTradingCycle({
    String symbol = 'BTCUSDT',
  }) async {
    try {
      final response = await _dio.post(
        '/trading/run-cycle',
        data: {'symbol': symbol},
      );
      
      return TradingCycleResponse.fromJson(response.data);
    } on DioException catch (e) {
      _logger.e('Ошибка при запуске торгового цикла: ${e.message}');
      throw _handleDioError(e);
    } catch (e) {
      _logger.e('Неожиданная ошибка: $e');
      throw Exception('Не удалось запустить торговый цикл');
    }
  }

  Future<List<Trade>> getTradeHistory() async {
    try {
      final response = await _dio.get('/trades');
      
      final List<dynamic> data = response.data;
      return data.map((json) => Trade.fromJson(json)).toList();
    } on DioException catch (e) {
      _logger.e('Ошибка при получении истории сделок: ${e.message}');
      throw _handleDioError(e);
    } catch (e) {
      _logger.e('Неожиданная ошибка: $e');
      throw Exception('Не удалось получить историю сделок');
    }
  }

  Future<MarketData> getLatestMarketData({
    String symbol = 'BTCUSDT',
  }) async {
    try {
      final response = await _dio.get(
        '/market/latest',
        queryParameters: {'symbol': symbol},
      );
      
      return MarketData.fromJson(response.data);
    } on DioException catch (e) {
      _logger.e('Ошибка при получении рыночных данных: ${e.message}');
      throw _handleDioError(e);
    } catch (e) {
      _logger.e('Неожиданная ошибка: $e');
      throw Exception('Не удалось получить рыночные данные');
    }
  }

  Exception _handleDioError(DioException e) {
    switch (e.type) {
      case DioExceptionType.connectionTimeout:
      case DioExceptionType.sendTimeout:
      case DioExceptionType.receiveTimeout:
        return Exception('Превышено время ожидания. Проверьте подключение к интернету.');
      case DioExceptionType.badResponse:
        return Exception('Ошибка сервера: ${e.response?.statusCode}');
      case DioExceptionType.cancel:
        return Exception('Запрос был отменён');
      default:
        return Exception('Ошибка сети. Проверьте подключение.');
    }
  }
}
```

### Пример TradingCycleProvider (Riverpod)

```dart
@riverpod
class TradingCycleNotifier extends _$TradingCycleNotifier {
  @override
  FutureOr<TradingCycleResponse?> build() {
    return null; // Начальное состояние
  }

  Future<void> runCycle({String symbol = 'BTCUSDT'}) async {
    state = const AsyncLoading();
    
    final apiService = ref.read(apiServiceProvider);
    
    state = await AsyncValue.guard(() async {
      return await apiService.runTradingCycle(symbol: symbol);
    });
  }

  Future<void> refresh() async {
    final currentSymbol = state.value?.marketData.symbol ?? 'BTCUSDT';
    await runCycle(symbol: currentSymbol);
  }
}

@riverpod
ApiService apiService(ApiServiceRef ref) {
  final baseUrl = ref.watch(settingsProvider).apiBaseUrl;
  return ApiService(baseUrl: baseUrl);
}
```

Эти примеры помогут тебе быстрее начать разработку и понять ожидаемую структуру кода!

---

## Дополнительные примеры для визуализации агентов

### Пример AgentCard виджета (для демонстрации агентов)

```dart
class AgentCard extends StatelessWidget {
  final String agentName;
  final String status;
  final String input;
  final String output;
  final String action;
  final Color statusColor;
  final bool isActive;

  const AgentCard({
    required this.agentName,
    required this.status,
    required this.input,
    required this.output,
    required this.action,
    required this.statusColor,
    this.isActive = false,
  });

  @override
  Widget build(BuildContext context) {
    return Card(
      elevation: isActive ? 8 : 2,
      margin: EdgeInsets.symmetric(horizontal: 16, vertical: 8),
      color: isActive ? Colors.blue.shade50 : null,
      child: Padding(
        padding: EdgeInsets.all(16),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                Text(
                  agentName,
                  style: Theme.of(context).textTheme.titleLarge?.copyWith(
                    fontWeight: FontWeight.bold,
                  ),
                ),
                Container(
                  padding: EdgeInsets.symmetric(horizontal: 12, vertical: 4),
                  decoration: BoxDecoration(
                    color: statusColor,
                    borderRadius: BorderRadius.circular(12),
                  ),
                  child: Text(
                    status,
                    style: TextStyle(
                      color: Colors.white,
                      fontSize: 12,
                      fontWeight: FontWeight.bold,
                    ),
                  ),
                ),
              ],
            ),
            SizedBox(height: 12),
            _buildDataRow('← Вход:', input, Icons.arrow_downward),
            SizedBox(height: 8),
            _buildDataRow('⚙ Действие:', action, Icons.settings),
            SizedBox(height: 8),
            _buildDataRow('→ Выход:', output, Icons.arrow_upward),
          ],
        ),
      ),
    );
  }

  Widget _buildDataRow(String label, String value, IconData icon) {
    return Row(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        Icon(icon, size: 16, color: Colors.grey),
        SizedBox(width: 8),
        Expanded(
          child: RichText(
            text: TextSpan(
              style: TextStyle(color: Colors.black87, fontSize: 14),
              children: [
                TextSpan(
                  text: '$label ',
                  style: TextStyle(fontWeight: FontWeight.bold),
                ),
                TextSpan(text: value),
              ],
            ),
          ),
        ),
      ],
    );
  }
}
```

### Пример AgentFlowIndicator виджета (стрелки между агентами)

```dart
class AgentFlowIndicator extends StatefulWidget {
  final int activeStep; // 0, 1, 2 или 3
  
  const AgentFlowIndicator({required this.activeStep});

  @override
  _AgentFlowIndicatorState createState() => _AgentFlowIndicatorState();
}

class _AgentFlowIndicatorState extends State<AgentFlowIndicator>
    with SingleTickerProviderStateMixin {
  late AnimationController _controller;

  @override
  void initState() {
    super.initState();
    _controller = AnimationController(
      duration: Duration(milliseconds: 1000),
      vsync: this,
    )..repeat();
  }

  @override
  void dispose() {
    _controller.dispose();
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: EdgeInsets.symmetric(vertical: 16),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        children: [
          _buildStep('Monitor', 0),
          _buildArrow(0),
          _buildStep('Decide', 1),
          _buildArrow(1),
          _buildStep('Execute', 2),
        ],
      ),
    );
  }

  Widget _buildStep(String label, int step) {
    final isActive = widget.activeStep == step;
    final isCompleted = widget.activeStep > step;

    return Column(
      children: [
        Container(
          width: 50,
          height: 50,
          decoration: BoxDecoration(
            color: isActive
                ? Colors.blue
                : isCompleted
                    ? Colors.green
                    : Colors.grey.shade300,
            shape: BoxShape.circle,
          ),
          child: Center(
            child: isCompleted
                ? Icon(Icons.check, color: Colors.white)
                : Text(
                    '${step + 1}',
                    style: TextStyle(
                      color: isActive ? Colors.white : Colors.grey.shade600,
                      fontWeight: FontWeight.bold,
                      fontSize: 18,
                    ),
                  ),
          ),
        ),
        SizedBox(height: 8),
        Text(
          label,
          style: TextStyle(
            fontWeight: isActive ? FontWeight.bold : FontWeight.normal,
            color: isActive ? Colors.blue : Colors.grey.shade700,
          ),
        ),
      ],
    );
  }

  Widget _buildArrow(int step) {
    final isActive = widget.activeStep == step + 1;

    return AnimatedBuilder(
      animation: _controller,
      builder: (context, child) {
        return Icon(
          Icons.arrow_forward,
          color: isActive ? Colors.blue : Colors.grey.shade400,
          size: isActive ? 24 + (_controller.value * 4) : 24,
        );
      },
    );
  }
}
```

### Пример использования на Dashboard

```dart
class DashboardScreen extends ConsumerWidget {
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final cycleState = ref.watch(tradingCycleProvider);
    
    return Scaffold(
      appBar: AppBar(
        title: Text('Multi-Agent Trading System'),
        actions: [
          IconButton(
            icon: Icon(Icons.timeline),
            onPressed: () => Navigator.push(
              context,
              MaterialPageRoute(builder: (_) => SystemFlowScreen()),
            ),
          ),
          IconButton(
            icon: Icon(Icons.settings),
            onPressed: () => Navigator.push(
              context,
              MaterialPageRoute(builder: (_) => SettingsScreen()),
            ),
          ),
        ],
      ),
      body: cycleState.when(
        loading: () => Center(child: CircularProgressIndicator()),
        error: (error, stack) => ErrorDisplay(error: error),
        data: (cycle) => RefreshIndicator(
          onRefresh: () => ref.read(tradingCycleProvider.notifier).refresh(),
          child: SingleChildScrollView(
            physics: AlwaysScrollableScrollPhysics(),
            child: Column(
              children: [
                // Flow indicator showing active step
                AgentFlowIndicator(activeStep: 3), // All completed
                
                SizedBox(height: 16),
                
                // Market Monitor Agent Card
                AgentCard(
                  agentName: 'Market Monitoring Agent',
                  status: 'Completed',
                  statusColor: Colors.green,
                  input: 'Request to monitor ${cycle.marketData.symbol}',
                  action: 'Fetched price \$${cycle.marketData.price}, calculated indicators',
                  output: 'Price: \$${cycle.marketData.price}, SMA_10: ${cycle.marketData.indicators.sma10.toStringAsFixed(2)}',
                  isActive: false,
                ),
                
                // Arrow indicator
                Padding(
                  padding: EdgeInsets.symmetric(vertical: 8),
                  child: Icon(Icons.arrow_downward, size: 32, color: Colors.blue),
                ),
                
                // Decision Maker Agent Card
                AgentCard(
                  agentName: 'Decision-Making Agent',
                  status: 'Completed',
                  statusColor: Colors.green,
                  input: 'Market data with ${cycle.marketData.indicators.toString()}',
                  action: 'ML Model processed features, predicted ${cycle.decision.action}',
                  output: 'Decision: ${cycle.decision.action} (${(cycle.decision.confidence * 100).toInt()}% confidence)',
                  isActive: false,
                ),
                
                // Arrow indicator
                Padding(
                  padding: EdgeInsets.symmetric(vertical: 8),
                  child: Icon(Icons.arrow_downward, size: 32, color: Colors.blue),
                ),
                
                // Execution Agent Card
                AgentCard(
                  agentName: 'Execution Agent',
                  status: cycle.execution.status,
                  statusColor: cycle.execution.status == 'FILLED' 
                      ? Colors.green 
                      : Colors.orange,
                  input: 'Decision: ${cycle.decision.action} at \$${cycle.marketData.price}',
                  action: 'Simulated trade execution, logged to database',
                  output: 'Order ${cycle.execution.orderId}, Status: ${cycle.execution.status}',
                  isActive: false,
                ),
                
                SizedBox(height: 24),
                
                // Quick stats
                Padding(
                  padding: EdgeInsets.all(16),
                  child: Card(
                    child: Padding(
                      padding: EdgeInsets.all(16),
                      child: Column(
                        crossAxisAlignment: CrossAxisAlignment.start,
                        children: [
                          Text(
                            'Cycle Summary',
                            style: Theme.of(context).textTheme.titleMedium,
                          ),
                          Divider(),
                          _buildSummaryRow('Cycle ID', '#${cycle.cycleId}'),
                          _buildSummaryRow('Timestamp', _formatTime(cycle.timestamp)),
                          _buildSummaryRow('Symbol', cycle.marketData.symbol),
                          _buildSummaryRow('Final Action', cycle.decision.action),
                          _buildSummaryRow('Execution Status', cycle.execution.status),
                        ],
                      ),
                    ),
                  ),
                ),
              ],
            ),
          ),
        ),
      ),
      floatingActionButton: FloatingActionButton.extended(
        onPressed: () => ref.read(tradingCycleProvider.notifier).runCycle(),
        label: Text('Запустить новый цикл'),
        icon: Icon(Icons.play_arrow),
        backgroundColor: Colors.blue,
      ),
    );
  }

  Widget _buildSummaryRow(String label, String value) {
    return Padding(
      padding: EdgeInsets.symmetric(vertical: 4),
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: [
          Text(label, style: TextStyle(color: Colors.grey.shade600)),
          Text(value, style: TextStyle(fontWeight: FontWeight.bold)),
        ],
      ),
    );
  }

  String _formatTime(DateTime dt) {
    return '${dt.hour}:${dt.minute.toString().padLeft(2, '0')}';
  }
}
```

Эти дополнительные примеры помогут создать наглядную визуализацию работы мультиагентной системы!
